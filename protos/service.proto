syntax = "proto3";

package VetulusService;


message ProtoFile {
    bytes data = 1;
    MetaData meta = 2;
}

message MetaData {
    string name = 1;
}

message Ack {
    bool done = 1;
}

service ProtoServer {

    rpc Load(ProtoFile) returns (Ack) {}
}
